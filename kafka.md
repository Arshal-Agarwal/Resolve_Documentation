# ⚠️ Superseded — see `Contracts/kafka.md`

This file was an early draft of the Kafka producer/consumer map. It has been superseded by **`Contracts/kafka.md`**, which is now the single source of truth for:

- Topic ownership and the producer/consumer map
- The shared event envelope and per-topic JSON Schemas
- Delivery semantics, retry/backoff, and DLQ naming
- The previously-open decisions this draft left unresolved (event-type casing, partition key, `resolve.document.scanned`, Notification's filtered subscription to `resolve.case.statuschanged`, etc.)

Keeping two files with the same topic map under different states of resolution was itself causing confusion — this file is kept only as a redirect. Do not edit this file; edit `Contracts/kafka.md` instead.
